/**
 * The {@code private_base} package contains classes that support the
 * interpreter without requiring it to be initialised. (Specifically,
 * they may be used before the Python type system is in working order,
 * and without causing it to initialise.)
 * <p>
 * Classes {@code public} in this package are not intended to be
 * accessible to users. They are public so that the implementation can
 * use them.
 */
package uk.co.farowl.vsj3.evo1._base;

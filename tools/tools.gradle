/*
 * tools subproject (tools used to generate or evaluate the runtime).
 *
 */

plugins {
    id 'antlr'
}

dependencies {
    antlr "org.antlr:antlr4:4.7" // use ANTLR version 4
    testCompile 'junit:junit:4.12'
}



generateGrammarSource {
    arguments += ["-visitor"]
}


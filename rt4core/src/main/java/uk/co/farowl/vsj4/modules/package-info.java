/**
 * The {@code modules} package contains standard library modules
 * implemented in Java. As far as possible, the class name is equal to
 * the Python module name, to aid to navigation. They will mostly be
 * used from Python via an {@code import}. Use from Java is also
 * possible, where classes are {@code public} for extension writers and
 * those embedding the interpreter in an application.
 */
package uk.co.farowl.vsj4.modules;

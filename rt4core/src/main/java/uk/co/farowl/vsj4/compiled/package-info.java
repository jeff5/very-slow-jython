/**
 * The {@code compiled} package contains the ephemeral generated Java
 * classes that define the behaviour of Python code objects and the
 * apparatus for introducing them into the JVM. No code outside the
 * runtime references the classes directly although user code may
 * encounter instances of them as Python {@code frame} objects.
 * <p>
 * This package is not exported.
 */
package uk.co.farowl.vsj4.compiled;

/**
 * The {@code subclass} package contains ephemeral generated
 * representations of subclasses defined in Python. THere has to be a
 * representation for each materially different sub-class of each
 * built-in type. No code outside the runtime references the classes
 * directly although it will handle instances of them as instances of
 * the Python subclass.
 * <p>
 * This package is not exported.
 */
package uk.co.farowl.vsj4.subclass;
